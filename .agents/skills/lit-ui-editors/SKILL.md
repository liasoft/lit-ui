---
name: lit-ui-editors
description: >-
  Use when building specialised admin screens with @liasoft/lit-ui — an editable record table (DNS records, env vars, forwarders), a filterable tailing log pane, a config diff, a code/file display, a file tree, or an API-keys/credentials panel with one-time secret reveal. Covers lia-code-block, lia-log-viewer, lia-diff-view, lia-record-editor, lia-file-tree, lia-api-keys-panel and the exported diff functions.
---

# lit-ui editors

Read and follow [the shared editors skill](../../../plugins/lit-ui/skills/editors/SKILL.md).
Resolve references and resources relative to that source file's directory;
preserve any script's documented working directory.
When the source names another family skill, use the corresponding
lit-ui-<family> adapter or read its shared source under plugins/lit-ui/skills/
relative to the repository root. The Claude plugin remains the shared source;
do not duplicate its component documentation here.
