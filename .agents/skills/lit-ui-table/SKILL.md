---
name: lit-ui-table
description: >-
  Build data listings with @liasoft/lit-ui tables — server-paged listings (lia-table), client-side sortable/filterable arrays (lia-data-table), standalone pagers (lia-pagination), and the cell-renderer catalogue (badge, bytes, datetime, progressbar…). Load when rendering tabular data, row actions, bulk selection, or row-detail expansion in an admin UI.
---

# lit-ui table

Read and follow [the shared table skill](../../../plugins/lit-ui/skills/table/SKILL.md).
Resolve references and resources relative to that source file's directory;
preserve any script's documented working directory.
When the source names another family skill, use the corresponding
lit-ui-<family> adapter or read its shared source under plugins/lit-ui/skills/
relative to the repository root. The Claude plugin remains the shared source;
do not duplicate its component documentation here.
