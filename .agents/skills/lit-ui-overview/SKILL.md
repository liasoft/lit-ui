---
name: lit-ui-overview
description: >-
  Entry point for building UI with @liasoft/lit-ui, the Bootstrap 5 light-DOM web-components kit (lia-* tags). Load this whenever you see lia-* elements in templates, @liasoft/lit-ui in package.json or imports, or when asked to build admin pages, tables, forms, dashboards or auth screens in a repo that uses this kit. Maps every use case to its component family and the family skill to load next.
---

# lit-ui overview

Read and follow [the shared overview skill](../../../plugins/lit-ui/skills/overview/SKILL.md).
Resolve references and resources relative to that source file's directory;
preserve any script's documented working directory.
When the source names another family skill, use the corresponding
lit-ui-<family> adapter or read its shared source under plugins/lit-ui/skills/
relative to the repository root. The Claude plugin remains the shared source;
do not duplicate its component documentation here.
