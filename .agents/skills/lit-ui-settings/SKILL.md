---
name: lit-ui-settings
description: >-
  Build a settings/preferences screen with @liasoft/lit-ui — grouped settings with a live filter, a scroll-following anchor rail, and one save bar. Load this when building any "configure the system" page (server settings, account preferences, integration config) instead of hand-rolling forms and anchor navigation.
---

# lit-ui settings

Read and follow [the shared settings skill](../../../plugins/lit-ui/skills/settings/SKILL.md).
Resolve references and resources relative to that source file's directory;
preserve any script's documented working directory.
When the source names another family skill, use the corresponding
lit-ui-<family> adapter or read its shared source under plugins/lit-ui/skills/
relative to the repository root. The Claude plugin remains the shared source;
do not duplicate its component documentation here.
