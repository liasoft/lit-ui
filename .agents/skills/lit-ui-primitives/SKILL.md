---
name: lit-ui-primitives
description: >-
  Use when placing basic UI building blocks with @liasoft/lit-ui — buttons and toolbars, badges, progress meters, cards, empty states, spinners, copy buttons, tooltips/popovers, key-value info rows, or hover-lookup identifiers — or when wiring the ActionDescriptor / lia-action / confirm contract that every action button in the kit shares.
---

# lit-ui primitives

Read and follow [the shared primitives skill](../../../plugins/lit-ui/skills/primitives/SKILL.md).
Resolve references and resources relative to that source file's directory;
preserve any script's documented working directory.
When the source names another family skill, use the corresponding
lit-ui-<family> adapter or read its shared source under plugins/lit-ui/skills/
relative to the repository root. The Claude plugin remains the shared source;
do not duplicate its component documentation here.
